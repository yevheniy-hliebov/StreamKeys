import 'package:flutter/material.dart';

class KeyEditorLoader extends StatelessWidget {
  const KeyEditorLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
