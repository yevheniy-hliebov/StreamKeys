import 'package:flutter/material.dart';

class GridArea extends StatelessWidget {
  const GridArea({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Grid Area');
  }
}